Compile `even.cpp' with output in `even':

icpx even.cpp -o even

then run:

./even

This program takes interactive input.
Input from file:

./even < input.txt

Also output to file:

./even < input.txt > output.txt

